# Local config

Put kabu Station API secrets in local files under this directory.

Example:

```text
config/kabu_password.txt
```

`config/*.txt` and `config/*.json` are ignored by git. Do not commit real API
passwords or tokens.
